[package]
name = "sqlite-downgrade-candidate"
version = "0.0.0"
edition = "2021"

[workspace]

[dependencies]
rusqlite = { version = "=0.39.0", features = ["bundled"] }
sqlx = { version = "=0.9.0", default-features = false, features = ["sqlite", "runtime-tokio"] }
